// Browser Dependencies


// jQuery - some hackery involved due to issues with the window object in Node
// TODO: make this a proper NPM dependency instead of using a CDN
script.
    if (typeof module === 'object') {
        window.module = module;
        module = undefined;
    }

script(
    src='https://code.jquery.com/jquery-3.1.1.min.js'
    integrity='sha256-hVVnYaiADRTO2PzUGmuLJr8BLUSjGIZsDYGmIJLv2b8='
    crossorigin='anonymous')

script.
    if (window.module) {
        module = window.module;
    }


// Bootstrap
// TODO: make this a proper NPM dependency instead of using a CDN
link(
    rel='stylesheet'
    href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css'
    integrity='sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u'
    crossorigin='anonymous'
)


// Ace Editor
script(src=`${process.env.ROOT}/node_modules/ace-builds/src-noconflict/ace.js`)
