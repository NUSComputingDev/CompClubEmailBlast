doctype html
html
    head
        title #{env.APPNAME}
        include browser_dependencies.pug
        link(rel='stylesheet' href=`${env.CSS}/entrypoint.css`)
        script(src=`${env.SRC}/entrypoint.js`)
    body
        div(class='jumbotron')
            h1 #{env.APPNAME}
        nav(class='navbar navbar-inverse')
            div(class='container-fluid')
                ul(class='nav navbar-nav')
                    li(class='active') #[a(href='#') Start]
                    li #[a(href='#') Place Content]
                    li #[a(href='#') Configure Settings]
                    li #[a(href='#') Send]
